extends layout

block content
    h1 #{statusCode} #{statusPhrase}

    if data
        pre= data

    each error in errors
        if error.stack
            pre= error.stack
        else if Array.isArray(error)
            each sub in error
                if sub.stack
                    pre= sub.stack
                else
                    pre= sub
        else
            pre= error