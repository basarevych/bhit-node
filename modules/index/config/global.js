/**
 * Repo-saved module configuration
 */
module.exports = {
    // Load module classes and services, path names
    autoload: [
        'module.js',
        'routes',
    ],

    // View directories
    views: [
        'views',
    ],

    // Static directories
    static: [
        'public',
    ]
};
